# Unit Test Placeholder

The scaffold includes test targets called out in the spec, but no runner has been wired yet.

Planned coverage:

- translation queue batching and cache hits
- navigation target selection
- auto-pause scheduling from deterministic cue timing
- adapter event normalization
- IndexedDB cache reads and writes
